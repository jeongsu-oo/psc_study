"""
============================================================
 클래스 연습문제 6문제
============================================================

 [푸는 방법]
   각 문제 아래의 빈 줄에 클래스를 작성하세요.
   그 아래 [테스트 코드] 의 주석을 풀어서 실행하면
   제대로 만들었는지 확인할 수 있습니다.
   [출력] 과 똑같이 나오면 성공입니다.


============================================================
"""


# =============================================================
# [문제 1] 강아지
# =============================================================
#
# 클래스 이름
#   Dog
#
# 만들 때 받을 값
#   name    이름
#   age     나이
#
# 그 외에 먹은 간식 개수를 기억하는 값이 필요하다.
# 처음에는 0으로 시작한다.
#
# 메서드
#   bark()
#     "멍멍! 나는 초코야" 형태로 출력
#
#   eat(count)
#     간식을 count 개 먹는다
#     "초코가 간식 2개를 먹었다 (총 2개)" 형태로 출력
#
#   birthday()
#     나이를 1 늘리고 "초코의 생일! 이제 4살" 형태로 출력
#
#   is_puppy()
#     나이가 2살 이하면 True, 아니면 False 를 돌려준다
#
#   show()
#     "초코 (3살, 성견)  간식 2개" 형태로 출력
#     2살 이하면 "성견" 자리에 "강아지" 가 나온다
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   초코 (3살, 성견)  간식 0개
#   멍멍! 나는 초코야
#   초코가 간식 2개를 먹었다 (총 2개)
#   초코가 간식 3개를 먹었다 (총 5개)
#   초코의 생일! 이제 4살
#   초코 (4살, 성견)  간식 5개
#
#   콩이 (1살, 강아지)  간식 0개
#   콩이는 강아지인가? True
# -------------------------------------------------------------

# d1 = Dog("초코", 3)
# d1.show()
# d1.bark()
# d1.eat(2)
# d1.eat(3)
# d1.birthday()
# d1.show()
#
# print()
#
# d2 = Dog("콩이", 1)
# d2.show()
# print("콩이는 강아지인가?", d2.is_puppy())


# =============================================================
# [문제 2] 성적표
# =============================================================
#
# 클래스 이름
#   Report
#
# 만들 때 받을 값
#   name    학생 이름
#
# 그 외에 점수 목록을 담을 값이 필요하다.
# 처음에는 비어 있다.
#
# 메서드
#   add(subject, score)
#     과목과 점수를 추가한다
#     0~100 범위를 벗어나면 "잘못된 점수: 150" 을 출력하고 추가하지 않는다
#     추가되면 "국어 90점 등록" 형태로 출력
#
#   average()
#     평균을 돌려준다 (소수 첫째 자리까지)
#     점수가 하나도 없으면 0 을 돌려준다
#
#   grade()
#     평균으로 등급을 돌려준다
#     90 이상 A / 80 이상 B / 70 이상 C / 그 외 D
#
#   best()
#     가장 높은 점수의 과목 이름과 점수를 함께 돌려준다
#     점수가 없으면 None 을 돌려준다
#
#   show()
#     아래 형태로 출력한다
#       [김철수 성적표]
#         국어 90점
#         영어 85점
#         수학 100점
#       평균 91.7 (A)
#       최고 과목: 수학 100점
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   국어 90점 등록
#   영어 85점 등록
#   잘못된 점수: 150
#   수학 100점 등록
#   [김철수 성적표]
#     국어 90점
#     영어 85점
#     수학 100점
#   평균 91.7 (A)
#   최고 과목: 수학 100점
#
#   [이영희 성적표]
#   평균 0 (D)
#   최고 과목: 없음
# -------------------------------------------------------------

# r = Report("김철수")
# r.add("국어", 90)
# r.add("영어", 85)
# r.add("과학", 150)
# r.add("수학", 100)
# r.show()
#
# print()
#
# r2 = Report("이영희")
# r2.show()


# =============================================================
# [문제 3] 자판기
# =============================================================
#
# 클래스 이름
#   VendingMachine
#
# 만들 때 받을 값
#   없음
#
# 그 외에 필요한 값
#   투입 금액 (처음에는 0)
#   판매할 음료 목록. 아래와 같이 정해져 있다.
#     콜라 1500원 재고 3개
#     사이다 1300원 재고 2개
#     물 800원 재고 5개
#
# 메서드
#   insert(money)
#     돈을 넣는다
#     "1,000원 투입 (총 1,000원)" 형태로 출력
#
#   buy(name)
#     음료를 산다. 아래 순서로 확인한다.
#       없는 음료면          "그런 음료는 없습니다: 커피"
#       재고가 0이면         "품절입니다: 콜라"
#       투입 금액이 모자라면  "금액이 부족합니다 (부족액 500원)"
#       살 수 있으면         "콜라 나왔습니다 (거스름돈 500원)"
#                          그리고 재고를 1 줄이고 투입 금액을 0으로 되돌린다
#
#   show()
#     아래 형태로 재고 목록을 출력한다
#       [자판기]
#         콜라 1,500원 (재고 3개)
#         사이다 1,300원 (재고 2개)
#         물 800원 (재고 5개)
#       투입 금액: 0원
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   [자판기]
#     콜라 1,500원 (재고 3개)
#     사이다 1,300원 (재고 2개)
#     물 800원 (재고 5개)
#   투입 금액: 0원
#   1,000원 투입 (총 1,000원)
#   금액이 부족합니다 (부족액 500원)
#   1,000원 투입 (총 2,000원)
#   콜라 나왔습니다 (거스름돈 500원)
#   그런 음료는 없습니다: 커피
#   [자판기]
#     콜라 1,500원 (재고 2개)
#     사이다 1,300원 (재고 2개)
#     물 800원 (재고 5개)
#   투입 금액: 0원
# -------------------------------------------------------------

# v = VendingMachine()
# v.show()
# v.insert(1000)
# v.buy("콜라")
# v.insert(1000)
# v.buy("콜라")
# v.buy("커피")
# v.show()


# =============================================================
# [문제 4] 도서 대출
# =============================================================
#
# 클래스 이름
#   Book
#
# 만들 때 받을 값
#   title     제목
#   author    저자
#
# 그 외에 필요한 값
#   대출 상태 (처음에는 대출 가능)
#   빌린 사람 이름 (처음에는 없음)
#   총 대출 횟수 (처음에는 0)
#
# 메서드
#   borrow(who)
#     who 라는 사람이 빌린다
#     이미 대출 중이면 "이미 대출 중입니다 (대출자: 김철수)" 출력
#     빌리면 대출 횟수를 1 늘리고
#     "사피엔스 대출 완료 (대출자: 김철수)" 형태로 출력
#
#   give_back()
#     반납한다
#     대출 중이 아니면 "대출 중이 아닙니다" 출력
#     반납하면 "사피엔스 반납 완료 (반납자: 김철수)" 형태로 출력하고
#     빌린 사람 이름을 지운다
#
#   show()
#     대출 중일 때    "사피엔스 / 유발 하라리 / 대출중 (김철수) / 누적 1회"
#     대출 가능할 때  "사피엔스 / 유발 하라리 / 대출가능 / 누적 1회"
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   사피엔스 / 유발 하라리 / 대출가능 / 누적 0회
#   대출 중이 아닙니다
#   사피엔스 대출 완료 (대출자: 김철수)
#   사피엔스 / 유발 하라리 / 대출중 (김철수) / 누적 1회
#   이미 대출 중입니다 (대출자: 김철수)
#   사피엔스 반납 완료 (반납자: 김철수)
#   사피엔스 대출 완료 (대출자: 박민수)
#   사피엔스 / 유발 하라리 / 대출중 (박민수) / 누적 2회
# -------------------------------------------------------------

# b = Book("사피엔스", "유발 하라리")
# b.show()
# b.give_back()
# b.borrow("김철수")
# b.show()
# b.borrow("이영희")
# b.give_back()
# b.borrow("박민수")
# b.show()


# =============================================================
# [문제 5] 직원과 관리자 (상속)
# =============================================================
#
# 클래스 두 개를 만든다.
#
# -------------------------------------------------------------
# 5-1. Employee
# -------------------------------------------------------------
#   만들 때 받을 값
#     name        이름
#     base_pay    기본급
#     years       근속 연수
#
#   메서드
#     get_position()
#       직급 이름을 돌려준다. 일반 직원은 "사원"
#
#     get_bonus_rate()
#       보너스 비율을 돌려준다. 일반 직원은 0.1
#
#     get_bonus()
#       보너스를 돌려준다
#       보너스 = 기본급 x 보너스 비율 + 근속수당
#       근속수당은 근속 연수 1년당 100,000원
#       소수점은 버린다
#
#     get_total()
#       기본급 + 보너스를 돌려준다
#
#     show()
#       "김철수 (사원, 3년)  기본급 3,000,000원  보너스 600,000원  실수령 3,600,000원"
#       형태로 출력
#
# -------------------------------------------------------------
# 5-2. Manager
# -------------------------------------------------------------
#   Employee 를 물려받는다.
#
#   달라지는 점
#     직급이 "팀장" 이다.
#     보너스 비율이 0.3 이다.
#
#   get_bonus(), get_total(), show() 는 새로 만들지 않는다.
#   물려받은 것을 그대로 쓴다.
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   김철수 (사원, 3년)  기본급 3,000,000원  보너스 600,000원  실수령 3,600,000원
#   이영희 (팀장, 7년)  기본급 3,000,000원  보너스 1,600,000원  실수령 4,600,000원
#
#   [전체 명단]
#   김철수 (사원, 3년)  기본급 3,000,000원  보너스 600,000원  실수령 3,600,000원
#   이영희 (팀장, 7년)  기본급 3,000,000원  보너스 1,600,000원  실수령 4,600,000원
#   박민수 (사원, 1년)  기본급 2,500,000원  보너스 350,000원  실수령 2,850,000원
#   총 인건비: 11,050,000원
#   최고 실수령: 이영희
# -------------------------------------------------------------

# e1 = Employee("김철수", 3000000, 3)
# e1.show()
#
# m1 = Manager("이영희", 3000000, 7)
# m1.show()
#
# print()
# print("[전체 명단]")
# staff = [e1, m1, Employee("박민수", 2500000, 1)]
#
# total = 0
# best = staff[0]
# for s in staff:
#     s.show()
#     total = total + s.get_total()
#     if s.get_total() > best.get_total():
#         best = s
#
# print(f"총 인건비: {total:,}원")
# print(f"최고 실수령: {best.name}")


# =============================================================
# [문제 6] 계좌와 저축계좌 (상속 + 캡슐화)
# =============================================================
#
# 클래스 두 개를 만든다.
#
# -------------------------------------------------------------
# 6-1. Account
# -------------------------------------------------------------
#   만들 때 받을 값
#     owner      주인 이름
#     balance    잔액
#
#   잔액은 밖에서 직접 바꿀 수 없도록 밑줄을 붙여 보관한다.
#   거래 내역도 함께 기록한다. 처음에는 비어 있다.
#
#   메서드
#     get_balance()
#       잔액을 돌려준다
#
#     deposit(amount)
#       0 이하면 "입금액은 0보다 커야 합니다" 출력하고 입금하지 않는다
#       입금하면 "10,000원 입금 (잔액 60,000원)" 형태로 출력하고
#       거래 내역에 "입금 10000" 을 기록한다
#
#     withdraw(amount)
#       잔액이 부족하면 "잔액 부족 (현재 60,000원)" 출력하고 출금하지 않는다
#       출금하면 "20,000원 출금 (잔액 40,000원)" 형태로 출력하고
#       거래 내역에 "출금 20000" 을 기록한다
#
#     history()
#       거래 내역을 아래 형태로 출력한다
#         [거래 내역]
#           1. 입금 10000
#           2. 출금 20000
#
#     show()
#       "김철수님 계좌  잔액 40,000원  거래 2건" 형태로 출력
#
# -------------------------------------------------------------
# 6-2. SavingsAccount
# -------------------------------------------------------------
#   Account 를 물려받는다.
#
#   만들 때 받을 값
#     owner, balance 에 더해 rate (이자율) 를 하나 더 받는다
#
#   메서드
#     add_interest()
#       이자를 계산해서 입금한다
#       이자 = 잔액 x 이자율 (소수점은 버린다)
#       "이자 5,000원 지급" 을 출력한 뒤 입금 처리한다
#
#     withdraw(amount)
#       저축계좌는 출금할 때마다 수수료 1,000원을 뗀다
#       즉 잔액에서 (출금액 + 1000) 이 빠진다
#       잔액이 부족하면 "잔액 부족 (현재 105,000원)" 출력
#       출금하면 아래 두 줄을 출력한다
#         출금 수수료 1,000원
#         20,000원 출금 (잔액 84,000원)
#       거래 내역에는 "출금 20000" 만 기록한다
#
# -------------------------------------------------------------
# -------------------------------------------------------------
# [테스트 코드]
# -------------------------------------------------------------
# [출력]
#   김철수님 계좌  잔액 50,000원  거래 0건
#   10,000원 입금 (잔액 60,000원)
#   입금액은 0보다 커야 합니다
#   20,000원 출금 (잔액 40,000원)
#   잔액 부족 (현재 40,000원)
#   김철수님 계좌  잔액 40,000원  거래 2건
#   [거래 내역]
#     1. 입금 10000
#     2. 출금 20000
#
#   이영희님 계좌  잔액 100,000원  거래 0건
#   이자 5,000원 지급
#   5,000원 입금 (잔액 105,000원)
#   출금 수수료 1,000원
#   20,000원 출금 (잔액 84,000원)
#   이영희님 계좌  잔액 84,000원  거래 2건
# -------------------------------------------------------------

# a = Account("김철수", 50000)
# a.show()
# a.deposit(10000)
# a.deposit(-5000)
# a.withdraw(20000)
# a.withdraw(999999)
# a.show()
# a.history()
#
# print()
#
# s = SavingsAccount("이영희", 100000, 0.05)
# s.show()
# s.add_interest()
# s.withdraw(20000)
# s.show()