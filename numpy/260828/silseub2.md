# ============================================================
# 파이썬 → NumPy → pandas 실습  [2단계 · 중급]
# 주제: 제조 설비 센서 데이터 다루기
# ============================================================
#
# 초급에서 손으로 하던 일을 함수와 클래스로 묶고, 마지막에 NumPy로 넘어갑니다.
# 문제 7부터는 같은 폴더의 설비온도기록.csv 가 필요합니다.
# 문제는 위에서 아래로 이어집니다. 앞에서 만든 함수와 클래스를 뒤에서 다시 씁니다.
 
import math
import random
import numpy as np
 
온도 = [71.2, 68.5, 75.9, 80.1, 66.3, 72.4, 69.8, 95.6, 70.0, 73.1, 68.9, 71.5]
측정값문자 = ["71.2", "68.5", "이상", "80.1", "N/A"]
 
 
# ----------------------------------------
# 문제 1. 함수로 묶기
# ----------------------------------------
# 값 리스트를 받아 평균을 소수 둘째 자리로 돌려주는 함수 평균내기 를 만드세요.
# sum 을 쓰지 말고 반복문으로 더합니다.
# 온도 리스트와 [70.0, 72.0, 74.0] 에 각각 적용해 차례로 출력하세요.
# 초급 문제 8에서 여러 줄로 하던 일이 이제 이름 하나로 불립니다.
# 기대 출력: 73.61
#            72.0
 
 
# ----------------------------------------
# 문제 2. 기본값과 키워드 인자
# ----------------------------------------
# 값 하나를 받아 등급을 돌려주는 함수 등급판정(값, 주의=75, 이상=90) 을 만드세요.
# 이상 이면 '이상', 주의 이면 '주의', 아니면 '정상' 을 돌려줍니다.
# 온도 8번째, 3번째, 1번째 값의 등급을 한 줄에 출력하세요.
# 이어서 3번째 값을 주의 기준 80으로, 8번째 값을 이상 기준 100으로 다시 판정해
# 한 줄에 출력하세요. 기준을 바꾸면 등급이 어떻게 달라지는지 보세요.
# 기대 출력: 이상 주의 정상
#            정상 주의
 
 
# ----------------------------------------
# 문제 3. 값 여러 개 돌려주기
# ----------------------------------------
# 값 리스트를 받아 합계(소수 첫째 자리), 평균, 최댓값, 최솟값 네 개를 한꺼번에
# 돌려주는 함수 요약 을 만드세요. 최대·최소는 max·min 없이 반복문으로 구합니다.
# 온도에 적용해 네 값을 각각 변수로 받아 한 줄에 출력하세요.
# 기대 출력: 883.3 73.61 95.6 66.3
 
 
# ----------------------------------------
# 문제 4. 전역 변수와 지역 변수
# ----------------------------------------
# 함수 밖에 기준온도 = 75 를 두고, 함수 안에서 그 기준을 읽어
# 기준보다 큰 값이 몇 개인지 돌려주는 함수를 만드세요.
# 개수와 기준온도를 한 줄에 출력하세요.
# 함수 안에서 바깥 변수를 읽는 것은 되지만, 바꾸려면 따로 선언이 필요합니다.
# 기대 출력: 3 75
 
 
# ----------------------------------------
# 문제 5. 표준 라이브러리 쓰기
# ----------------------------------------
# math 로 16의 제곱근, 원주율(소수 넷째 자리), 73.61의 올림과 내림을 한 줄에 출력하세요.
# 이어서 random 의 시드를 42로 고정하고 0~11 사이 정수 3개를 한 줄에 출력하세요.
# 시드를 다시 42로 맞추고 똑같이 3개를 출력해, 두 줄이 같은지 확인하세요.
# 기대 출력: 4.0 3.1416 74 73
#            10 1 0
#            10 1 0
 
 
# ----------------------------------------
# 문제 6. 예외 처리
# ----------------------------------------
# 측정값문자에는 숫자로 바꿀 수 없는 값이 섞여 있습니다.
# 반복문으로 하나씩 실수로 바꾸되, 실패한 값은 프로그램을 멈추지 말고 따로 모으세요.
# 성공한 값 목록, 실패한 값 목록, 그리고 각각의 개수를 차례로 출력하세요.
# 기대 출력: [71.2, 68.5, 80.1]
#            ['이상', 'N/A']
#            3 2
 
 
# ----------------------------------------
# 문제 7. 파일을 직접 열어 읽기
# ----------------------------------------
# 같은 폴더의 설비온도기록.csv 를 with 문으로 열어 전체를 줄 단위로 읽으세요.
# 첫 줄을 쉼표로 쪼갠 헤더 목록, 데이터 줄 수, 그리고 첫 데이터 줄을 쪼갠 목록과
# 그 온도값을 실수로 바꾼 값을 차례로 출력하세요. 한글이 깨지지 않게 읽습니다.
# 기대 출력: ['회차', '온도', '진동', '압력']
#            24
#            ['1', '74.9', '28.68', '3.76'] 74.9
 
 
# ----------------------------------------
# 문제 8. 결과를 파일로 남기기
# ----------------------------------------
# 문제 7에서 읽은 줄들에서 온도만 실수로 뽑아 리스트로 만들고,
# '측정 24회 / 평균 73.9 / 최고 95.6' 형태의 한 줄 보고서를 만들어
# 점검보고서.txt 로 저장한 뒤, 그 파일을 다시 열어 내용을 출력하세요.
# 평균은 문제 1의 함수를 재사용합니다.
# 여기서 쓰는 온도는 CSV에서 읽은 24개입니다. 맨 위의 12개 리스트가 아닙니다.
# 기대 출력: 측정 24회 / 평균 73.9 / 최고 95.6
 
 
# ----------------------------------------
# 문제 9. 클래스 만들기
# ----------------------------------------
# 이름과 값 리스트를 받아 저장하는 센서 클래스를 만드세요.
# 평균을 돌려주는 평균 메서드와, '범용 센서 / 측정 12회 / 평균 73.61' 형태의
# 문자열을 돌려주는 소개 메서드를 넣습니다.
# 이름이 '범용', 값이 온도(맨 위 12개 리스트 - 문제 8의 24개가 아닙니다)인 객체를 만들어
# 이름과 평균을 한 줄에 출력하고,
# 이어서 소개 결과를 출력하세요.
# 기대 출력: 범용 73.61
#            범용 센서 / 측정 12회 / 평균 73.61
 
 
# ----------------------------------------
# 문제 10. 상속
# ----------------------------------------
# 센서를 물려받는 온도센서 클래스를 만드세요. 클래스 변수 한계 = 90 을 두고,
# 한계 이상인 값이 몇 개인지 돌려주는 이상개수 메서드를 추가합니다.
# 이름 '온도', 값 온도인 객체를 만들어 소개를 출력하고,
# 이어서 이상개수와 한계를 한 줄에 출력하세요.
# 소개 메서드는 만들지 않았는데도 쓸 수 있다는 점을 확인하세요.
# 기대 출력: 온도 센서 / 측정 12회 / 평균 73.61
#            1 90
 
 
# ----------------------------------------
# 문제 11. 오버라이딩
# ----------------------------------------
# 센서를 물려받는 진동센서 클래스를 만드세요. 한계는 35 이고,
# 소개 메서드를 '[진동] 진동 / 평균 33.12 / 한계 35' 형태로 다시 정의합니다.
# 값이 [30.1, 31.4, 41.2, 29.8] 인 객체를 만들어 소개를 출력하고,
# 이어서 문제 10의 온도센서 소개도 출력해 둘이 다른지 확인하세요.
# 마지막 줄에 진동센서 객체가 센서인지, 온도센서인지를 한 줄에 출력하세요.
# 기대 출력: [진동] 진동 / 평균 33.12 / 한계 35
#            온도 센서 / 측정 12회 / 평균 73.61
#            True False
 
 
# ----------------------------------------
# 문제 12. 서로 다른 객체를 같은 방법으로 다루기
# ----------------------------------------
# 온도센서 객체와 진동센서 객체를 한 리스트에 담고,
# 반복문으로 순서대로 소개를 출력하세요.
# 같은 이름의 메서드를 불렀는데 결과가 다르게 나오는 것이 오버라이딩의 힘입니다.
# 기대 출력: 온도 센서 / 측정 12회 / 평균 73.61
#            [진동] 진동 / 평균 33.12 / 한계 35
 
 
# ----------------------------------------
# 문제 13. NumPy 배열로 바꿔 보기
# ----------------------------------------
# 온도 리스트를 NumPy 배열로 바꿔 모양과 자료형을 한 줄에 출력하세요.
# 이어서 합계(소수 첫째 자리), 평균(소수 둘째 자리), 최댓값, 최솟값을 한 줄에 출력하세요.
# 마지막으로 모든 값에 3을 더한 결과를 소수 첫째 자리로 출력하세요.
# 초급 문제 8에서 여러 줄 걸리던 것이 여기서 몇 줄이 되는지 세어 보세요.
# 기대 출력: (12,) float64
#            883.3 73.61 95.6 66.3
#            [74.2 71.5 78.9 83.1 69.3 75.4 72.8 98.6 73.  76.1 71.9 74.5]
 
 
# ----------------------------------------
# 문제 14. 불리언 인덱싱
# ----------------------------------------
# 배열에서 75를 넘는 값만 골라 출력하고, 그 위치 번호를 출력하고,
# 전체 대비 비율(%)을 소수 첫째 자리로 출력하세요.
# 초급 문제 15와 결과가 같아야 합니다. 몇 줄로 줄었는지 비교해 보세요.
# 기대 출력: [75.9 80.1 95.6]
#            [2 3 7]
#            25.0
 
 
# ----------------------------------------
# 문제 15. z-점수 이상 탐지를 함수로 - 오늘의 마무리
# ----------------------------------------
# 설비온도기록.csv 를 NumPy 배열로 읽고(첫 줄은 헤더) 온도 열만 따로 뽑으세요.
# 배열 모양과 온도 개수를 한 줄에 출력합니다.
# 이어서 값 배열과 임계값(기본 3)을 받아 아래 네 단계를 밟는 함수 이상탐지 를 만드세요.
#   1) 평균 구하기  2) 표준편차 구하기  3) z-점수 계산  4) 임계값 넘는 값 골라내기
# 함수는 평균, 표준편차(각각 소수 둘째 자리), 걸린 값들, 걸린 위치를 돌려줍니다.
# 온도 열에 기본 임계값으로 적용해 평균·표준편차를 한 줄, 걸린 값·위치·개수를 한 줄
# 출력하세요.
# 이어서 임계값을 2로 낮춰 다시 적용해 걸린 값·위치·개수를 한 줄 출력하고,
# 전체 대비 비율(%)을 소수 첫째 자리로 출력하세요.
# 임계값을 낮췄더니 무엇이 더 걸렸는지 확인하세요. 이 함수가 고급 단계의 출발점입니다.
# 기대 출력: (24, 4) 24
#            73.9 5.81
#            [95.6] [7] 1
#            [95.6 88.4] [ 7 15] 2
#            8.3